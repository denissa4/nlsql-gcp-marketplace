# Test deployer image. `mpdev verify` builds and runs this to install the tester
# chart alongside the app and watch its Pod for a zero exit status.
FROM gcr.io/cloud-marketplace-tools/k8s/deployer_helm/onbuild
