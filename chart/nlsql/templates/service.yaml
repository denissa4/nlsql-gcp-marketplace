apiVersion: v1
kind: Service
metadata:
  name: {{ include "nlsql.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "nlsql.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nlsql.selectorLabels" . | nindent 4 }}
